$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

<% if @contratossolicitud.contratossoldetalles.count == 1 %>
	$("#contratossoldetalles").html("<%= j render 'tabla' %>");
<% else %>
	$("#tbody_contratossoldetalles").prepend("<%= j render 'tr', contratossoldetalle: @contratossoldetalle %>");
<% end %>

$(".clean").val("").trigger('change');

$("#contratossoldetalle_form").collapse("hide");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

$(".form-group").removeClass("has-error");
$(".form-group > .help-block.help-block-error").remove();

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);