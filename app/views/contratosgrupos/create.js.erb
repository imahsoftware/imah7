$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

<% if @contrato.contratosgrupos.count == 1 %>
	$("#contratosgrupos").html("<%= j render 'tabla' %>");
<% else %>
	$("#tbody_contratosgrupos").prepend("<%= j render 'tr', contratosgrupo: @contratosgrupo %>");
<% end %>

$(".clean").val("").trigger('change');

$("#contratosgrupo_form").collapse("hide");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

$(".form-group").removeClass("has-error");
$(".form-group > .help-block.help-block-error").remove();

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
