$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#contratosgrupo_<%= @contratosgrupo.id %>").replaceWith("<%= j render 'tr', contratosgrupo: @contratosgrupo %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
