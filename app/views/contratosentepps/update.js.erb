$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#contratosentepp_<%= @contratosentepp.id %>").replaceWith("<%= j render 'tr', contratosentepp: @contratosentepp %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
