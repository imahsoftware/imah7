<%= simple_form_for( @contratosentepp, remote: true) do |f| %>
  <div class="bs-callout">
    <div class="row">
      <div class="col-md-12">
        <%= f.input :contrato_id, collection: select_contratos.collect { |p| [p.datos.to_s, p.id] }, input_html: { class: 'select2', style: "width: 100%" }, as: :select, include_blank: true, label: 'Contrato' %>
      </div>
    </div>
    <br/>
    <div class="row">
      <div class="col-md-6">
        <% if @contratosentepp.new_record? %>
          <%= button_tag "#{t :cancelar}", type: 'button', data: { target: '#formulariosllave_form' }, class: 'cancel_button btn btn-flat btn-default pull-left', aria_hidden: true %>
        <% else %>
          <%= link_to "#{t :cancelar}", contratosentepp_path(@contratosentepp), class: 'btn btn-flat btn-default pull-left', aria_hidden: true, remote: true %>
        <% end %>
      </div>
      <div class="col-md-6">
        <%= f.button :submit, "#{t :guardar}", class: "btn btn-danger pull-right" %>
      </div>
    </div>
  </div>
<% end %>

















