$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#veriserviciosicompromiso_<%= @veriserviciosicompromiso.id %>").replaceWith("<%= j render 'tr', veriserviciosicompromiso: @veriserviciosicompromiso %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
