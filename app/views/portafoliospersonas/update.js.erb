$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#portafoliospersona_<%= @portafoliospersona.id %>").replaceWith("<%= j render 'tr', portafoliospersona: @portafoliospersona %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
