$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#contratosperestado_<%= @contratosperestado.id %>").replaceWith("<%= j render 'tr', contratosperestado: @contratosperestado %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
