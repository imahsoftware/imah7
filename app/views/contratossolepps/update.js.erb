$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#contratossolepp_<%= @contratossolepp.id %>").replaceWith("<%= j render 'tr', contratossolepp: @contratossolepp %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
