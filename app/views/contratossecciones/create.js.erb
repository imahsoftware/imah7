$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

<% if @contrato.contratossecciones.count == 1 %>
	$("#contratossecciones").html("<%= j render 'tabla' %>");
<% else %>
	$("#tbody_contratossecciones").prepend("<%= j render 'tr', contratosseccion: @contratosseccion %>");
<% end %>

$(".clean").val("").trigger('change');

$("#contratosseccion_form").collapse("hide");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

$(".form-group").removeClass("has-error");
$(".form-group > .help-block.help-block-error").remove();

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
