$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#contratossolnota_<%= @contratossolnota.id %>").replaceWith("<%= j render 'tr', contratossolnota: @contratossolnota %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
