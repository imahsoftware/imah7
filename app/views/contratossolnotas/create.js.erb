$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

<% if @contratossolicitud.contratossolnotas.count == 1 %>
	$("#contratossolnotas").html("<%= j render 'tabla' %>");
<% else %>
	$("#tbody_contratossolnotas").prepend("<%= j render 'tr', contratossolnota: @contratossolnota %>");
<% end %>

$(".clean").val("").trigger('change');

$("#contratossolnota_form").collapse("hide");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

$(".form-group").removeClass("has-error");
$(".form-group > .help-block.help-block-error").remove();

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
