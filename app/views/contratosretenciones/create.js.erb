$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

<% if @contrato.contratosretenciones.count == 1 %>
	$("#contratosretenciones").html("<%= j render 'tabla' %>");
<% else %>
	$("#tbody_contratosretenciones").prepend("<%= j render 'tr', contratosretencion: @contratosretencion %>");
<% end %>

$(".clean").val("").trigger('change');

$("#contratosretencion_form").collapse("hide");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

$(".form-group").removeClass("has-error");
$(".form-group > .help-block.help-block-error").remove();

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
