$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#contratosretencion_<%= @contratosretencion.id %>").replaceWith("<%= j render 'tr', contratosretencion: @contratosretencion %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
