$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#contratosperpronota_<%= @contratosperpronota.id %>").replaceWith("<%= j render 'tr', contratosperpronota: @contratosperpronota %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
