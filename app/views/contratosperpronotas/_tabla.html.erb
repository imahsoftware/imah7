<div class="table-responsive">
  <table class="table table-bordered table-condensed table-striped">
    <thead class="thcolor">
    <tr>
      <td width="10%"></td>
      <th class="text-left text-table" width="15%">Fecha</th>
      <th class="text-left text-table">Nota</th>
    </tr>
    </thead>
    <tbody id="tbody_contratosperpronotas">
    <% @contratosperproceso.contratosperpronotas.order(id: :desc).each do |contratosperpronota| %>
      <%= render 'contratosperpronotas/tr', contratosperpronota: contratosperpronota %>
    <% end %>
    </tbody>
  </table>
</div>
