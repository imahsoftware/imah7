$("#contratosperpronota_form").collapse("hide");

$("#contratosperpronota_form").empty();

<% if @active_record.present? %>
	$("#contratosperpronota_<%= @active_record.id %>").replaceWith("<%= j render 'tr', contratosperpronota: @active_record %>");
<% end %>

$("#contratosperpronota_<%= @contratosperpronota.id %>").html("<td colspan='12'><%= j render 'form' %></td>");

$("#contratosperpronota_<%= @contratosperpronota.id %>").addClass("active_contratosperpronota");

$(".select2").select2();

$(".tooltip").tooltip("hide");

$('[data-toggle="tooltip"]').tooltip();
$(".datetimepicker").datetimepicker();
$('.datepicker').datepicker({
    dayNamesMin: [ "Dom", "Lun", "Mar", "Mie", "Jue", "Vie", "Sáb" ],
    monthNamesShort: [ "Ene", "Feb", "Mar", "Abr", "May", "Jun", "Jul", "Ago", "Sep", "Oct", "Nov", "Dic" ],
    dateFormat: "yy-mm-dd",
    yearRange: '2000:2027',
    changeMonth: true,
    changeYear: true,
});

$('.uppercaseInput').keyup(function () {
    $(this).val($(this).val().toUpperCase());
});