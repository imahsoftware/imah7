$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#contratosprefdebito_<%= @contratosprefdebito.id %>").replaceWith("<%= j render 'tr', contratosprefdebito: @contratosprefdebito %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
