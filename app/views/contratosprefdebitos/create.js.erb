$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

<% if @contrato.contratosprefdebitos.count == 1 %>
	$("#contratosprefdebitos").html("<%= j render 'tabla' %>");
<% else %>
	$("#tbody_contratosprefdebitos").prepend("<%= j render 'tr', contratosprefdebito: @contratosprefdebito %>");
<% end %>

$(".clean").val("").trigger('change');

$("#contratosprefdebito_form").collapse("hide");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

$(".form-group").removeClass("has-error");
$(".form-group > .help-block.help-block-error").remove();

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
