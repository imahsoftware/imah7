$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#contratospernota_<%= @contratospernota.id %>").replaceWith("<%= j render 'tr', contratospernota: @contratospernota %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
