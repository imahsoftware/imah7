$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#contratosperprodoc_<%= @contratosperprodoc.id %>").replaceWith("<%= j render 'tr', contratosperprodoc: @contratosperprodoc %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
