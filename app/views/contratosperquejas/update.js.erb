$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#contratosperqueja_<%= @contratosperqueja.id %>").replaceWith("<%= j render 'tr', contratosperqueja: @contratosperqueja %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
