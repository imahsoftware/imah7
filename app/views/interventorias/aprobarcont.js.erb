$('#mensaje_otp').html("<%= j render 'menus/mensaje_otp_registro_generico' %>");

<% if @validacion %>
  $(document).ready(function() {
    setTimeout(function() {
      location.href = '<%= interventorias_path %>';
    }, 2000);
  });
<% end %>
