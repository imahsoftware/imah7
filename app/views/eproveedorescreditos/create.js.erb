$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

<% if @eproveedor.eproveedorescreditos.count == 1 %>
	$("#eproveedorescreditos").html("<%= j render 'tabla' %>");
<% else %>
	$("#tbody_eproveedorescreditos").prepend("<%= j render 'tr', eproveedorescredito: @eproveedorescredito %>");
<% end %>

$(".clean").val("").trigger('change');

$("#eproveedorescredito_form").collapse("hide");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

$(".form-group").removeClass("has-error");
$(".form-group > .help-block.help-block-error").remove();

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
