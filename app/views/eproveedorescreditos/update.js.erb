$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#eproveedorescredito_<%= @eproveedorescredito.id %>").replaceWith("<%= j render 'tr', eproveedorescredito: @eproveedorescredito %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
