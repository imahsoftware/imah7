
<%
  isadmin = is_admin
  user = User.find(isadmin)
  isportafolio = is_portafolio
  issygma = is_sygma
%>
<aside class="main-sidebar">
  <section class="sidebar">
    <!-- Sidebar Menu -->
    <ul class="sidebar-menu" data-widget="tree">
      <li>
        <%= link_to users_path do %>
          <i class="fa fa-arrow-left text-danger"></i> Regresar a usuarios
        <% end %>
      </li>
      <li class="header"><%=t :menu%></li>
      <!-- Optionally, you can add icons to the links -->
      <li class="<%= active_class(edit_user_path(etapa: "A", id: @user.id)) %>">
        <%= link_to edit_user_path(etapa: "A", id: @user.id) do %>
          <i class="fa fa-bullseye"></i> <span>Datos básicos</span>
        <% end %>
      </li>
    </ul>
  </section>
</aside>
