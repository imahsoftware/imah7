<div class="table-responsive">
  <table class="table table-bordered table-condensed table-striped">
	  <thead class="thcolor">
      <tr>
        <td width="5%"></td>
        <th class="text-left">Fecha</th>
        <th width="80%" class="text-justify">Usuario Asignado</th>
      </tr>
    </thead>
	  <tbody id="tbody_contratosactnovusers">
	    <% @contratosactnovedad.contratosactnovusers.order(id: :desc).each do |contratosactnovuser| %>
	      <%= render 'contratosactnovusers/tr', contratosactnovuser: contratosactnovuser %>
	    <% end %>
	  </tbody>
	</table>
</div>
