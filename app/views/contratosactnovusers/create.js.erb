$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

<% if @contratosactnovedad.contratosactnovusers.count == 1 %>
	$("#contratosactnovusers").html("<%= j render 'tabla' %>");
<% else %>
	$("#tbody_contratosactnovusers").prepend("<%= j render 'tr', contratosactnovuser: @contratosactnovuser %>");
<% end %>

$(".clean").val("").trigger('change');

$("#contratosactnovuser_form").collapse("hide");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

$(".form-group").removeClass("has-error");
$(".form-group > .help-block.help-block-error").remove();

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
