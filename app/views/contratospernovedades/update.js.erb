$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#contratospernovedad_<%= @contratospernovedad.id %>").replaceWith("<%= j render 'tr', contratospernovedad: @contratospernovedad %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
