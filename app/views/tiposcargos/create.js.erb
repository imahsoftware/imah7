$(".alert_flash").fadeOut(1000, function() {
	$(this).remove();
});

<% if Tiposcargo.count == 1 %>
	$("#tiposcargos").html("<%= j render 'tabla' %>");
<% else %>
	$("#tbody_tiposcargos").prepend("<%= j render 'tr', tiposcargo: @tiposcargo %>");
<% end %>

$(".clean").val("").trigger('change');

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

$(".form-group").removeClass("has-error");
$(".form-group > .help-block.help-block-error").remove();

setTimeout(function() {
	$(".alert_flash").fadeOut(1000, function() {
		$(this).remove();
	});
}, 5000);
