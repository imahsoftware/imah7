$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

<% if @contrato.contratosactividades.count == 1 %>
	$("#contratosactividades").html("<%= j render 'tabla' %>");
<% else %>
	$("#tbody_contratosactividades").prepend("<%= j render 'tr', contratosactividad: @contratosactividad %>");
<% end %>

$(".clean").val("").trigger('change');

$("#contratosactividad_form").collapse("hide");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

$(".form-group").removeClass("has-error");
$(".form-group > .help-block.help-block-error").remove();

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
