$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#contratoscargosnota_<%= @contratoscargosnota.id %>").replaceWith("<%= j render 'tr', contratoscargosnota: @contratoscargosnota %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
