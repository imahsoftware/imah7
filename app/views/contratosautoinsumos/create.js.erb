$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

<% if @contrato.contratosautoinsumos.count == 1 %>
	$("#contratosautoinsumos").html("<%= j render 'tabla' %>");
<% else %>
	$("#tbody_contratosautoinsumos").prepend("<%= j render 'tr', contratosautoinsumo: @contratosautoinsumo %>");
<% end %>

$(".clean").val("").trigger('change');

$("#contratosautoinsumo_form").collapse("hide");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

$(".form-group").removeClass("has-error");
$(".form-group > .help-block.help-block-error").remove();

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
