$(".alert_flash").fadeOut(1000, function() {
	$(this).remove();
});

$("#centro_<%= @centro.id %>").replaceWith("<%= j render 'tr', centro: @centro %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(1000, function() {
		$(this).remove();
	});
}, 5000);
