<% title 'centros' %>

<div class="box box-primary">
  <div class="box-header with-border">
    <h3 class="box-title">
      <%# if is_auth_c('centro') %>
        <%= link_to new_centro_path, remote: true, id: 'new_centro_button', data: { toggle: 'tooltip' }, title: 'Agregar' do %>
          <i class="fa fa-plus fa-fw text-success" aria-hidden="true"></i>
        <% end %>
      <%# end %>
      Centros de Trabajo
    </h3>
    <div class="box-tools pull-right">
      <%= search_form_for @q do |f| %>
        <div class="input-group input-group-sm" style="width: 180px;">
          <%= f.search_field :descripcion_cont, class: 'form-control pull-right', placeholder: 'Buscar...' %>
          <div class="input-group-btn">
            <%= button_tag(type: "submit", class: "btn btn-default") do %>
              <i class="fa fa-search"></i>
            <% end %>
          </div>
        </div>
      <% end %>
    </div><!-- /.box-tools -->

  </div><!-- /.box-header -->
  <div class="flash_messages"></div>
  <div class="box-body">
    <div class="row">
      <div class="col-md-12">
        <div id="centro_form" class="collapse"></div>
        <div id="centros">
          <% if @centros.present? %>
        		<%= render 'centros/tabla' %>
        	<% else %>
        		<div class="alert alert-danger"><h4><i class="icon fa fa-info"></i> <%=t :atencion%></h4><%=t :atencion_msj%></div>
        	<% end %>
        </div>
      </div>
    </div>
  </div>
</div>
