<% if @contratosprefechascarnet_todo.present? %>
  <div class="box-body">
    <table class="table table-bordered table-condensed table-striped">
      <thead>
      <tr class="thcolor">
        <th class="text-left" width="5%"></th>
        <th class="text-left" width="25%">Empleado</th>
        <th class="text-left">Contrato</th>
      </tr>
      </thead>
      <tbody>
      <% @contratosprefechascarnet_todo.each do |contratosperfecha| %>
        <%= render 'menus/tr_carnet', contratosperfecha: contratosperfecha, p: @p %>
      <% end %>
      </tbody>
    </table>
    <div class="box-tools pull-right">
      <%= will_paginate @contratosprefechascarnet_todo, :param_name => "car_#{@p.downcase.to_s}", renderer: BootstrapPagination::Rails %>
    </div><!-- /.box-tools -->
  </div>
<% else %>
  <div class="alert alert-danger">
    <h4><i class="icon fa fa-info"></i> <%= t :atencion %></h4><%= t :atencion_msj %> </div>
<% end %>