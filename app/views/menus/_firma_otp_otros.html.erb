<div class="row">
  <div class="col-md-12">
    <% Controlfirma.where("user_firma = #{is_admin} and estado = 'PENDIENTE'").limit(1).each do |controlfirma| %>
      <%= render "menus/control_firma_digital", controlfirma: controlfirma %>
    <% end %>
  </div>
</div>
