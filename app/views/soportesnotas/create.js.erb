$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

<% if @soporte.soportesnotas.count == 1 %>
	$("#soportesnotas").html("<%= j render 'tabla' %>");
<% else %>
	$("#tbody_soportesnotas").prepend("<%= j render 'tr', soportesnota: @soportesnota %>");
<% end %>

$(".clean").val("").trigger('change');

$("#soportesnota_form").collapse("hide");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

$(".form-group").removeClass("has-error");
$(".form-group > .help-block.help-block-error").remove();

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
