$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#soportesnota_<%= @soportesnota.id %>").replaceWith("<%= j render 'tr', soportesnota: @soportesnota %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
