$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#veriserviciosuser_<%= @veriserviciosuser.id %>").replaceWith("<%= j render 'tr', veriserviciosuser: @veriserviciosuser %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
