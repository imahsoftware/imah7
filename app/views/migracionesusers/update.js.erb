$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#migracionesuser_<%= @migracionesuser.id %>").replaceWith("<%= j render 'tr', migracionesuser: @migracionesuser %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
