$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

<% if @user.migracionesusers.count == 1 %>
	$("#migracionesusers").html("<%= j render 'tabla' %>");
<% else %>
	$("#migracionesusers").html("<%= j render 'tabla' %>");
<% end %>

$(".clean").val("").trigger('change');

$("#migracionesuser_form").collapse("hide");

$("#migracionesuser_form").empty();

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

$(".form-group").removeClass("has-error");
$(".form-group > .help-block.help-block-error").remove();

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
