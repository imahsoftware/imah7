$(".alert_flash").fadeOut(1000, function() {
	$(this).remove();
});

$("#tiposdocumento_<%= @tiposdocumento.id %>").replaceWith("<%= j render 'tr', tiposdocumento: @tiposdocumento %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages2").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(1000, function() {
		$(this).remove();
	});
}, 5000);
