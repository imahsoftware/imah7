$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#contratoscaractividad_<%= @contratoscaractividad.id %>").replaceWith("<%= j render 'tr', contratoscaractividad: @contratoscaractividad %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
