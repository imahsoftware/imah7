$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#contratosactnovnota_<%= @contratosactnovnota.id %>").replaceWith("<%= j render 'tr', contratosactnovnota: @contratosactnovnota %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
