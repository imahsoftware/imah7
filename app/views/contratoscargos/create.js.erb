$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

<% if @contrato.contratoscargos.count == 1 %>
	$("#contratoscargos").html("<%= j render 'tabla' %>");
<% else %>
	$("#tbody_contratoscargos").prepend("<%= j render 'tr', contratoscargo: @contratoscargo %>");
<% end %>

$(".clean").val("").trigger('change');

$("#contratoscargo_form").collapse("hide");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

$(".form-group").removeClass("has-error");
$(".form-group > .help-block.help-block-error").remove();

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
