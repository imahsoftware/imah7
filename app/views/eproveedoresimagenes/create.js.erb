$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

<% if @eproveedor.eproveedoresimagenes.count == 1 %>
	$("#eproveedoresimagenes").html("<%= j render 'tabla' %>");
<% else %>
	$("#tbody_eproveedoresimagenes").prepend("<%= j render 'tr', eproveedoresimagen: @eproveedoresimagen %>");
<% end %>

$(".clean").val("").trigger('change');

$("#eproveedoresimagen_form").collapse("hide");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

$(".form-group").removeClass("has-error");
$(".form-group > .help-block.help-block-error").remove();

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
