$("#contratosenteppsdetalle_form").collapse("hide");

$("#contratosenteppsdetalle_form").empty();

<% if @active_record.present? %>
	$("#contratosenteppsdetalle_<%= @active_record.id %>").replaceWith("<%= j render 'tr', contratosenteppsdetalle: @active_record %>");
<% end %>

$("#contratosenteppsdetalle_<%= @contratosenteppsdetalle.id %>").html("<td colspan='12'><%= j render 'form' %></td>");

$("#contratosenteppsdetalle_<%= @contratosenteppsdetalle.id %>").addClass("active_contratosenteppsdetalle");

$(".select2").select2();

$(".tooltip").tooltip("hide");

$('[data-toggle="tooltip"]').tooltip();
$(".datetimepicker").datetimepicker();
$('.datepicker').datepicker({
    dayNamesMin: [ "Dom", "Lun", "Mar", "Mie", "Jue", "Vie", "Sáb" ],
    monthNamesShort: [ "Ene", "Feb", "Mar", "Abr", "May", "Jun", "Jul", "Ago", "Sep", "Oct", "Nov", "Dic" ],
    dateFormat: "yy-mm-dd",
    yearRange: '2000:2027',
    changeMonth: true,
    changeYear: true,
});