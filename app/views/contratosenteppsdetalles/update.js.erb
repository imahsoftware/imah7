$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#contratosenteppsdetalle_<%= @contratosenteppsdetalle.id %>").replaceWith("<%= j render 'tr', contratosenteppsdetalle: @contratosenteppsdetalle %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
