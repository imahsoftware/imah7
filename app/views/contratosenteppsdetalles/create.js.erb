$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

<% if @contratosentepp.contratosenteppsdetalles.count == 1 %>
	$("#contratosenteppsdetalles").html("<%= j render 'tabla' %>");
<% else %>
	$("#tbody_contratosenteppsdetalles").prepend("<%= j render 'tr', contratosenteppsdetalle: @contratosenteppsdetalle %>");
<% end %>

$(".clean").val("").trigger('change');

$("#contratosenteppsdetalle_form").collapse("hide");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

$(".form-group").removeClass("has-error");
$(".form-group > .help-block.help-block-error").remove();

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
