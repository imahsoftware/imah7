$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

<% if @formato.formatosvariables.count == 1 %>
	$("#formatosvariables").html("<%= j render 'tabla' %>");
<% else %>
	$("#tbody_formatosvariables").prepend("<%= j render 'tr', formatosvariable: @formatosvariable %>");
<% end %>

$(".clean").val("").trigger('change');

$("#formatosvariable_form").collapse("hide");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

$(".form-group").removeClass("has-error");
$(".form-group > .help-block.help-block-error").remove();

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
