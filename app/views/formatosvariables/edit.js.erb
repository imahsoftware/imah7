$("#formatosvariable_form").collapse("hide");

$("#formatosvariable_form").empty();

<% if @active_record.present? %>
	$("#formatosvariable_<%= @active_record.id %>").replaceWith("<%= j render 'tr', formatosvariable: @active_record %>");
<% end %>

$("#formatosvariable_<%= @formatosvariable.id %>").html("<td colspan='12'><%= j render 'form' %></td>");

$("#formatosvariable_<%= @formatosvariable.id %>").addClass("active_formatosvariable");

$(".select2").select2();

$(".tooltip").tooltip("hide");

$('[data-toggle="tooltip"]').tooltip();
$(".datetimepicker").datetimepicker();
$('.datepicker').datepicker({
    dayNamesMin: [ "Dom", "Lun", "Mar", "Mie", "Jue", "Vie", "Sáb" ],
    monthNamesShort: [ "Ene", "Feb", "Mar", "Abr", "May", "Jun", "Jul", "Ago", "Sep", "Oct", "Nov", "Dic" ],
    dateFormat: "yy-mm-dd",
    yearRange: '2000:2026',
    changeMonth: true,
    changeYear: true,
});