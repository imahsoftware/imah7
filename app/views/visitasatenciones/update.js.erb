$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#visitasatencion_<%= @visitasatencion.id %>").replaceWith("<%= j render 'tr', visitasatencion: @visitasatencion %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
