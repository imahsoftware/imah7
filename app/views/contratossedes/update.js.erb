$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#contratossede_<%= @contratossede.id %>").replaceWith("<%= j render 'tr', contratossede: @contratossede %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
