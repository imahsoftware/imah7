$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

<% if @contrato.contratossedes.count == 1 %>
	$("#contratossedes").html("<%= j render 'tabla' %>");
<% else %>
	$("#tbody_contratossedes").prepend("<%= j render 'tr', contratossede: @contratossede %>");
<% end %>

$(".clean").val("").trigger('change');

$("#contratossede_form").collapse("hide");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

$(".form-group").removeClass("has-error");
$(".form-group > .help-block.help-block-error").remove();

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
