<div class="box-header">
  <h3 class="box-title"><strong>Validaciones y examenes</strong></h3>
  <div class="box-tools pull-right">
    <% if is_auth_c('contratosperexamen') %>
      <%= link_to new_contratospersona_contratosperexamen_path(@contratospersona), id: 'new_contratosperexamen_button', class: 'btn btn-sm btn-warning', remote: true do %>
        <i class="fa fa-plus-square"> </i> <span><%=t :agregar%></span>
      <% end %>
    <% end %>
  </div>
</div>
<div class="box-body">
  <div class="flash_messages"></div>
  <div id="contratosperexamen_form" class="collapse"></div>
  <div id="contratosperexamenes">
    <% if @contratospersona.contratosperexamenes.present? %>
      <%= render 'contratosperexamenes/tabla' %>
    <% else %>
      <div class="alert alert-danger"><h4><i class="icon fa fa-info"></i> <%=t :atencion%></h4><%=t :atencion_msj%></div>
    <% end %>
  </div>
</div>
