$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

<% if @contratospersona.contratosperembargos.count == 1 %>
	$("#contratosperembargos").html("<%= j render 'tabla' %>");
<% else %>
	$("#tbody_contratosperembargos").prepend("<%= j render 'tr', contratosperembargo: @contratosperembargo %>");
<% end %>

$(".clean").val("").trigger('change');

$("#contratosperembargo_form").collapse("hide");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

$(".form-group").removeClass("has-error");
$(".form-group > .help-block.help-block-error").remove();

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
