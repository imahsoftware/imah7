$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

<% if @contratospersona.contratosperprestamos.count == 1 %>
	$("#contratosperprestamos").html("<%= j render 'tabla' %>");
<% else %>
	$("#tbody_contratosperprestamos").prepend("<%= j render 'tr', contratosperprestamo: @contratosperprestamo %>");
<% end %>

$(".clean").val("").trigger('change');

$("#contratosperprestamo_form").collapse("hide");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

$(".form-group").removeClass("has-error");
$(".form-group > .help-block.help-block-error").remove();

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
