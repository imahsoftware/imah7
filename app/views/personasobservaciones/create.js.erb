$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

<% if @persona.personasobservaciones.count == 1 %>
	$("#personasobservaciones").html("<%= j render 'tabla' %>");
<% else %>
	$("#tbody_personasobservaciones").prepend("<%= j render 'tr', personasobservacion: @personasobservacion %>");
<% end %>

$(".clean").val("").trigger('change');

$("#personasobservacion_form").collapse("hide");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

$(".form-group").removeClass("has-error");
$(".form-group > .help-block.help-block-error").remove();

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
