<%= simple_form_for([@veriservicio, @veriserviciositem], remote: true) do |f| %>
  <div class="bs-callout">
    <div class="row">
      <div class="col-md-12">
        <%= f.input :user_id, collection: User.where(["tipoconsulta not in ('PERSONA','CANDIDATO', 'RECTOR', 'METRO') and activo = 'S'"]).order(:nombre).collect {|p| [ p.nombrecompleto, p.id ] }, input_html: { class: 'select2 clean' }, as: :select,  include_blank: true, label: "Supervisor" %>
      </div>
    </div>

    <div class="row">
      <div class="col-md-6">
        <% if @veriserviciositem.new_record? %>
          <%= button_tag "#{t :cancelar}", type: 'button', data: { target: '#veriserviciositem_form' }, class: 'cancel_button btn btn-flat btn-default pull-left', aria_hidden: true %>
        <% else %>
          <%= link_to "#{t :cancelar}", veriservicio_veriserviciositem_path(@veriservicio, @veriserviciositem), class: 'btn btn-flat btn-default pull-left', aria_hidden: true, remote: true %>
        <% end %>
      </div>
      <div class="col-md-6">
        <%= f.button :submit, "#{t :guardar}", class: "btn btn-danger pull-right" %>
      </div>
    </div>
  </div>
<% end %>
<br/>