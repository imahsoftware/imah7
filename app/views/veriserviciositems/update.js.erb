$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#veriserviciositem_<%= @veriserviciositem.id %>").replaceWith("<%= j render 'tr', veriserviciositem: @veriserviciositem %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
