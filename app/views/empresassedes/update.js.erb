$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#empresassede_<%= @empresassede.id %>").replaceWith("<%= j render 'tr', empresassede: @empresassede %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
