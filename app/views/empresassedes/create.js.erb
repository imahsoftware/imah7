$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

<% if @empresa.empresassedes.count == 1 %>
	$("#empresassedes").html("<%= j render 'tabla' %>");
<% else %>
	$("#tbody_empresassedes").prepend("<%= j render 'tr', empresassede: @empresassede %>");
<% end %>

$(".clean").val("").trigger('change');

$("#empresassede_form").collapse("hide");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

$(".form-group").removeClass("has-error");
$(".form-group > .help-block.help-block-error").remove();

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
