$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#contratosmodificacion_<%= @contratosmodificacion.id %>").replaceWith("<%= j render 'tr', contratosmodificacion: @contratosmodificacion %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
