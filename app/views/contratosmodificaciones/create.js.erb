$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

<% if @contrato.contratosmodificaciones.count == 1 %>
	$("#contratosmodificaciones").html("<%= j render 'tabla' %>");
<% else %>
	$("#tbody_contratosmodificaciones").prepend("<%= j render 'tr', contratosmodificacion: @contratosmodificacion %>");
<% end %>

$(".clean").val("").trigger('change');

$("#contratosmodificacion_form").collapse("hide");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

$(".form-group").removeClass("has-error");
$(".form-group > .help-block.help-block-error").remove();

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
