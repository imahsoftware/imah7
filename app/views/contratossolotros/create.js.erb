$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

<% if @contratossolicitud.contratossolotros.count == 1 %>
	$("#contratossolotros").html("<%= j render 'tabla' %>");
<% else %>
	$("#tbody_contratossolotros").prepend("<%= j render 'tr', contratossolotro: @contratossolotro %>");
<% end %>

$(".clean").val("").trigger('change');

$("#contratossolotro_form").collapse("hide");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

$(".form-group").removeClass("has-error");
$(".form-group > .help-block.help-block-error").remove();

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
