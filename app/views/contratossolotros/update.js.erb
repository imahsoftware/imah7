$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#contratossolotro_<%= @contratossolotro.id %>").replaceWith("<%= j render 'tr', contratossolotro: @contratossolotro %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
