$("#contratossolotro_form").collapse("hide");

$("#contratossolotro_form").empty();

<% if @active_record.present? %>
	$("#contratossolotro_<%= @active_record.id %>").replaceWith("<%= j render 'tr', contratossolotro: @active_record %>");
<% end %>

$("#contratossolotro_<%= @contratossolotro.id %>").html("<td colspan='12'><%= j render 'form' %></td>");

$("#contratossolotro_<%= @contratossolotro.id %>").addClass("active_contratossolotro");

$(".select2").select2();

$(".tooltip").tooltip("hide");

$('[data-toggle="tooltip"]').tooltip();
$(".datetimepicker").datetimepicker();
$('.datepicker').datepicker({
    dayNamesMin: [ "Dom", "Lun", "Mar", "Mie", "Jue", "Vie", "Sáb" ],
    monthNamesShort: [ "Ene", "Feb", "Mar", "Abr", "May", "Jun", "Jul", "Ago", "Sep", "Oct", "Nov", "Dic" ],
    dateFormat: "yy-mm-dd",
    yearRange: '2000:2027',
    changeMonth: true,
    changeYear: true,
});