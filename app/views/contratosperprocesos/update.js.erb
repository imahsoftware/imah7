$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#contratosperproceso_<%= @contratosperproceso.id %>").replaceWith("<%= j render 'tr', contratosperproceso: @contratosperproceso %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
