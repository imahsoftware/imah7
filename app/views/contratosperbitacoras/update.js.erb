$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#contratosperbitacora_<%= @contratosperbitacora.id %>").replaceWith("<%= j render 'tr', contratosperbitacora: @contratosperbitacora %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
