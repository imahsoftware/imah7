<%= simple_form_for(@contratosperbitacora) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :contratospersona %>
    <%= f.input :estado %>
    <%= f.association :user %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
