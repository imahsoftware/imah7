$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

<% if @contratospersona.contratosperbitacoras.count == 1 %>
	$("#contratosperbitacoras").html("<%= j render 'tabla' %>");
<% else %>
	$("#tbody_contratosperbitacoras").prepend("<%= j render 'tr', contratosperbitacora: @contratosperbitacora %>");
<% end %>

$(".clean").val("").trigger('change');

$("#contratosperbitacora_form").collapse("hide");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

$(".form-group").removeClass("has-error");
$(".form-group > .help-block.help-block-error").remove();

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
