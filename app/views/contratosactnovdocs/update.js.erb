$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#contratosactnovdoc_<%= @contratosactnovdoc.id %>").replaceWith("<%= j render 'tr', contratosactnovdoc: @contratosactnovdoc %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
