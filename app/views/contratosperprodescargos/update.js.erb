$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#contratosperprodescargo_<%= @contratosperprodescargo.id %>").replaceWith("<%= j render 'tr', contratosperprodescargo: @contratosperprodescargo %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
