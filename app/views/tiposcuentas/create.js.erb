$(".alert_flash").fadeOut(1000, function() {
	$(this).remove();
});

<% if Tiposcuenta.count == 1 %>
	$("#tiposcuentas").html("<%= j render 'tabla' %>");
<% else %>
	$("#tbody_tiposcuentas").prepend("<%= j render 'tr', tiposcuenta: @tiposcuenta %>");
<% end %>

$(".clean").val("").trigger('change');

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

$(".form-group").removeClass("has-error");
$(".form-group > .help-block.help-block-error").remove();

setTimeout(function() {
	$(".alert_flash").fadeOut(1000, function() {
		$(this).remove();
	});
}, 5000);
