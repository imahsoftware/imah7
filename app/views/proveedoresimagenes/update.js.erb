$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#proveedoresimagen_<%= @proveedoresimagen.id %>").replaceWith("<%= j render 'tr', proveedoresimagen: @proveedoresimagen %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
