$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

<% if @proveedor.proveedoresimagenes.count == 1 %>
	$("#proveedoresimagenes").html("<%= j render 'tabla' %>");
<% else %>
	$("#tbody_proveedoresimagenes").prepend("<%= j render 'tr', proveedoresimagen: @proveedoresimagen %>");
<% end %>

$(".clean").val("").trigger('change');

$("#proveedoresimagen_form").collapse("hide");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

$(".form-group").removeClass("has-error");
$(".form-group > .help-block.help-block-error").remove();

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
